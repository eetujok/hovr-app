import { VideoRecordSyncGlobalActionContext } from "gadget-server";

/**
 * @param { VideoRecordSyncGlobalActionContext } context
 */
export async function run({ params, logger, api, connections }) {

};
